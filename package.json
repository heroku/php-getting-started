{
  "name": "pixelcoda-headless-search",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm -w apps/api run build && npm -w apps/worker run build && npm -w apps/widgets run build",
    "dev": "npm -w apps/api run dev",
    "lint": "eslint .",
    "typecheck": "tsc -b"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8",
    "tsx": "^4.20.5"
  }
}
